import React from "react";
import ReactDOM from 'react-dom';

window.addEventListener('DOMContentLoaded', ()=> {
    const root = document.getElementById('root');
    ReactDOM.render(<h2>Natty transformation no cyap</h2>, root )
});